import React from 'react';
import { Camera, Upload, RefreshCw, ChevronLeft, BookOpen, CheckCircle, AlertCircle, Loader2, Settings, X } from 'lucide-react';

export const Icons = {
  Camera,
  Upload,
  Refresh: RefreshCw,
  Back: ChevronLeft,
  Book: BookOpen,
  Check: CheckCircle,
  Alert: AlertCircle,
  Spinner: Loader2,
  Settings,
  X
};